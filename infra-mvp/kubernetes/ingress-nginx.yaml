apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mvp-ingress
  namespace: mvp
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: mvp.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: api-gateway
            port:
              number: 80

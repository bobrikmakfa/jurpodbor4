apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
  namespace: mvp
spec:
  selector:
    matchLabels:
      app: api-gateway
  template:
    metadata:
      labels:
        app: api-gateway
    spec:
      containers:
        - name: api-gateway
          image: nginx:alpine
          ports:
            - containerPort: 80
          volumeMounts:
            - name: conf
              mountPath: /etc/nginx/conf.d
      volumes:
        - name: conf
          configMap:
            name: api-gateway-conf
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: api-gateway-conf
  namespace: mvp
data:
  default.conf: |
    server {
      listen 80;

      location /auth/ {
        proxy_pass http://auth-stub/;
      }

      location /user/ {
        proxy_pass http://user-stub/;
      }

      location /lawyer/ {
        proxy_pass http://lawyer-stub/;
      }

      location /health {
        return 200;
      }
    }

---
apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: mvp
spec:
  selector:
    app: api-gateway
  ports:
    - port: 80
      targetPort: 80
